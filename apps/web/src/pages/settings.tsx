import { CustomSelect } from "../components/CustomSelect";
// Settings is a secondary route and must not be part of the first overview
// render. The implementation is re-exported through a lazy route boundary.
export { SettingsPage as default } from "../App";
